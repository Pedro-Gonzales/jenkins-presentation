pipeline{
    agent any

    tools {nodejs "node"}

    stages{
        stage ("teste"){
            steps{
                 sh 'echo: "Foi automatico"'
            }
        }

        stage ("Checkout do código"){
            steps{
                git url:  'https://github.com/Pedro-Gonzales/jenkins-presentation', branch: 'master'
            }
        }

        stage ('Construção do build node') {
            steps{
                sh 'npm install'
            }
        }

        stage ('Rodagem dos testes') {
            steps{
                sh 'npm test'
            }
        }
    }
}